# Decision Log

Этот файл записывает архитектурные и реализационные решения в формате списка.
2026-04-22 14:25:07 - Последняя редакция базового описания.

## Правила редактирования

* Записи в разделах должны быть отсортированы в обратном хронологическом порядке (самые новые сверху, старые внизу).
* Каждая запись должна начинаться с даты редакции в формате ГГГГ-ММ-ДД ЧЧ:ММ:СС.
2026-04-22 14:25:07 - Последняя редакция правил редактирования.

## Решение: Shared Domain Core и GAS bridges для resident-tracking
2026-04-22 14:25:07 - Решение: Развивать resident-tracking поверх общего foundation/domain core, а интеграцию с Google Apps Script и листами проводить через bridge-слой.
### Обоснование
* Resident-tracking use-cases переиспользуют общие foundation-построители, нормализацию и sheet contracts, поэтому изоляция RT в отдельный монолит создала бы дублирование и расхождение поведения.
* Bridge pattern снижает связность между domain-логикой и GAS runtime, что упрощает доработку use-cases, документации и тестовой поверхности.
### Детали реализации
* Resident-tracking сценарии опираются на общий DomainCore и вызываются через GAS entrypoints, включая [`syncResidentTracking()`](clasp/Резиденты%20Мабиз.js:1505).
* Меню в [`onOpen()`](clasp/Резиденты%20Мабиз.js:45) используется как orchestration boundary между пользовательским UI и bridge/use-case слоем.

## Решение: Resident-tracking как snapshot/read-model pipeline
2026-04-22 14:25:07 - Решение: Формировать resident-tracking витрины как набор snapshot/read-model builders, а не как прямую ad hoc аналитику по сырым данным.
### Обоснование
* RT-витрины требуют воспроизводимых, переиспользуемых представлений для timeline, token flows и issuer structure, а не одноразовых вычислений в UI.
* Snapshot/read-model pipeline позволяет согласованно обновлять downstream листы, повторно использовать header contracts и упростить doc-sync по operational flow.
### Детали реализации
* В pipeline выделены специализированные builders: [`buildResidentTimeline()`](clasp/Резиденты%20Мабиз.js:1631), [`buildTokenFlows()`](clasp/Резиденты%20Мабиз.js:1737), [`buildIssuerStructure()`](clasp/Резиденты%20Мабиз.js:1832).
* [`syncResidentTracking()`](clasp/Резиденты%20Мабиз.js:1505) закреплен как обязательный orchestration step, который обновляет upstream dataset и затем пересобирает downstream read-model листы.

## Решение: Account-metadata как параллельный intelligence layer
2026-04-22 14:25:07 - Решение: Держать account-metadata отдельно от resident-tracking read-models, используя его как параллельный intelligence layer для enrichment и последующей аналитики.
### Обоснование
* Account metadata меняется по своей траектории и не должен жестко смешиваться с RT snapshots, чтобы не ломать детерминированность витрин.
* Параллельный intelligence layer позволяет обогащать RT-контур без потери прозрачности data lineage и без смешения upstream и downstream обязанностей.
### Детали реализации
* Интеграционный commit `a8864c4` закрепляет синхронизацию account metadata и выравнивание operational docs как актуальный source of truth.
* RT pipeline использует account-metadata данные как дополнительный слой интерпретации, не заменяя foundation dataset и snapshot builders.

## Решение: Обязательный upstream-step перед RT-витринами
2026-04-22 14:25:07 - Решение: Считать resident-tracking витрины валидными только после обязательного обновления upstream dataset через [`syncResidentTracking()`](clasp/Резиденты%20Мабиз.js:1505).
### Обоснование
* Downstream views теряют консистентность, если строятся поверх устаревших foundation-данных, особенно при одновременной эволюции resident-tracking и account-metadata.
* Фиксация обязательного upstream-step в документации и меню снижает риск ручных запусков в некорректной последовательности.
### Детали реализации
* Operational flow закреплен в меню и документации: upstream sync, bootstrap/validation листов, затем пересборка RT snapshots/read-models.
* Source of truth для последовательности — реализованные use-cases и интеграционный commit `a8864c4`.

## Решение: Переход к слоистой архитектуре (Layered Architecture)
2026-04-01 13:45:00 - Решение: Разделение монолита на Domain Core, Adapters и Orchestration.
### Обоснование
*   Текущий монолит (2500+ строк) стал слишком хрупким и сложным для поддержки.
*   Resident Tracking требует глубокого анализа, который невозможно качественно реализовать в текущей структуре без риска поломок.
*   Необходимость разделения бизнес-логики и инфраструктуры (Sheets/Horizon) для обеспечения надежного тестирования.
### Детали реализации
*   Выделение Domain Core в чистые JS-функции (без GAS API).
*   Внедрение Repository Pattern для доступа к Sheets.
*   Разделение сценариев на Use Cases (Orchestration).

## Решение: Header-based Column Discovery (Repository Pattern)
2026-04-01 13:45:00 - Решение: Отказ от fixed-column индексов в пользу поиска по заголовкам.
### Обоснование
*   Системный риск поломок при изменении структуры Google Sheets пользователями.
*   Потребность в валидации схем листов перед началом операций.
### Детали реализации
*   Создание адаптера (SheetsRepository), который кэширует индексы колонок по их именам при инициализации.
*   Бросание исключений при отсутствии обязательных колонок.

## Записи MCP
2026-03-31 23:08:56 - Введен отдельный администраторский контур документации по эксплуатации таблицы и скрипта
### Решение
* Разделить документацию на два слоя:
  * пользовательский слой — сценарии запуска пунктов меню в [`docs/USER_MENU.md`](docs/USER_MENU.md:1);
  * администраторский слой — конфигурация, контракт листов/данных и операционный runbook в [`docs/ADMIN_SETUP_AND_CONFIG.md`](docs/ADMIN_SETUP_AND_CONFIG.md:1), [`docs/ADMIN_SHEETS_AND_DATA_CONTRACT.md`](docs/ADMIN_SHEETS_AND_DATA_CONTRACT.md:1), [`docs/ADMIN_RUNBOOK.md`](docs/ADMIN_RUNBOOK.md:1).
* Зафиксировать в админ-документации явные ограничения структуры данных: fixed-column assumptions, append-only/snapshot режимы и hidden state в user properties/cache.
### Обоснование
* Рост сложности скрипта привел к необходимости отдельной эксплуатационной документации, где описано не только «как запускать», но и «как устроены зависимости данных и что ломается при ручных изменениях таблицы».
* Снижение риска инцидентов при сопровождении: администратор получает формализованные процедуры настройки, диагностики и восстановления.
### Детали реализации
* Добавлены три документа в `docs/`: setup/config, data contract, runbook.
* README дополнен ссылками на администраторские документы.
* Явно документировано расхождение: `MEMO_PATTERNS_*` и `CLASSIFY_ENABLE` заданы в коде как hardcoded constants, а не читаются из `CONST`.

2026-03-31 21:42:38 - Синхронизация документации с фактическим entrypoint скрипта и выделение инструкции по меню в отдельный документ
### Решение
* Зафиксировать в документации, что основным рабочим скриптом является `clasp/Резиденты Мабиз.js`, а устаревшее упоминание `v3.js` удалить из README.
* Вынести подробную пользовательскую инструкцию по пунктам меню в отдельный файл `docs/USER_MENU.md` и связать его с README.
### Обоснование
* Снижение риска ошибок при onboarding и эксплуатации из-за расхождения между README и реальной структурой репозитория.
* Разделение «обзорной» и «операционной» документации: README остается компактным, а меню и сценарии запуска описаны детально в отдельном документе.
### Детали реализации
* README обновлен: ссылка на `v3.js` заменена на `clasp/Резиденты Мабиз.js`.
* Создан `docs/USER_MENU.md` с маппингом «пункт меню → функция → эффект», порядком запуска для новой таблицы и разделом по безопасности данных.

2026-01-21 12:32:55 - Заполнение Created by/Created data в ACCOUNTS через Horizon API
### Решение
* Добавить обновление столбцов E и F в ACCOUNTS на основе первой транзакции аккаунта из Horizon API.
* Для ссылок использовать URL, заданный в CONST как EXPLORER_TX_URL с fallback на base URL Horizon.
### Обоснование
* Требуется автоматизировать заполнение метаданных создания аккаунтов и обеспечить переход по ссылке на транзакцию.
### Детали реализации
* Реализованы функции getCreationTransactionDetails, formatCreationData, updateAccountCreationDetails и пункт меню.

2026-01-19 13:50:48 - Реализация `build_tags_tx` в отдельном подпроекте
### Решение
* Реализовать утилиту `build_tags_tx` (генератор XDR для manage_data) в отдельном, изолированном подпроекте (`build_tags_tx/`) внутри репозитория stellar-scrypt.
* Использовать `zsh` для скрипта, чтобы обеспечить строгую совместимость с требованиями ТЗ и спецификой `stellar-cli`.
### Обоснование
* Изоляция CLI-зависимого кода от основного проекта Google Apps Script.
* Обеспечение возможности быстрого запуска и тестирования генератора XDR.
* **Ключевое изменение в использовании CLI:** Обнаружено, что `stellar-cli 23.4.1` не поддерживает команду `stellar fees use --amount ...`. Комиссия теперь задается через флаг `--fee <amount>` в командах `stellar tx new` и `stellar tx operation add`. Также флаг `--seq-num` заменен на `--sequence-number`.
### Детали реализации
* Создан подпроект `build_tags_tx/` со структурой `src/`, `config/`, `out/`, `tests/`.
* Основной скрипт: `build_tags_tx/src/build_tags_tx.zsh`.
* Утилита HEX-кодирования: `build_tags_tx/src/utils.zsh` (с использованием `od` вместо `xxd`).
* Прогон полного тест-плана подтвердил корректность работы и соответствие ТЗ.

2026-01-18 23:13:03 - Добавлены функции upgradeTransfersSheet, upgradeResidentsSheet и upgradeExistingSheets, а также пункты меню для апгрейда листов
### Обоснование
* Обеспечение возможности быстрого расширения существующих листов без ручного вмешательства через меню
### Детали реализации
* Реализованы функции upgradeTransfersSheet, upgradeResidentsSheet, upgradeExistingSheets и добавлены пункты меню 'Апгрейд листа TRANSFERS', 'Апгрейд листа RESIDENTS', 'Апгрейд всех листов'

2026-01-16 22:44:51 - Обновление диагностики и дедупликации в syncStellarTransfers
### Обоснование
* Требуется выявить фильтр с максимальными потерями и избежать потерь операций в одной транзакции.
### Детали реализации
* Добавлены счетчики dropType/dropNonNative/dropInDate/dropMinAmount/dropRole/dropDuplicate/dropTokenFilter/dropNoTxHash в логи.
* Дедупликация по tx_hash+operation_id вместо только tx_hash.
* Добавлен дефолтный Horizon URL при пустом значении.

## Решение: Группировка дочерних счетов в отчетах
2025-12-19 13:23:26 - Решение: Группировка дочерних счетов в отчетах
### Обоснование
* Пользователь явно запросил группировку дочерних счетов под родительскими вместо добавления отдельной колонки "Родительский счет".

### Детали реализации
* Реализация будет осуществлена путем модификации функций формирования отчетов для обработки иерархической структуры счетов.
