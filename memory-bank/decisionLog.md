# Decision Log

This file records architectural and implementation decisions using a list format.
2025-12-19 13:23:26 - Log of updates made.

*

## Decision

* Отчеты должны группировать дочерние счета под родительскими, а не добавлять отдельную колонку "Parent Account".

## Rationale 

* Пользователь явно запросил группировку.

## Implementation Details

* Будет реализовано через модификацию функций формирования отчетов, чтобы они обрабатывали иерархическую структуру счетов. Будет реализовано через модификацию функций формирования отчетов, чтобы они обрабатывали иерархическую структуру счетов.